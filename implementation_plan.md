# 病毒感染過程動畫系統 (Infection Animation System)

這項計畫旨在為我們的 3D 病毒百科加入「動態感染過程」的視覺展示，讓使用者不僅能觀看病毒的靜態結構，還能親眼目睹它們是如何入侵宿主細胞的。

## User Review Required

> [!IMPORTANT]
> 由於這 30 種病毒的感染機制完全不同（例如：T4 噬菌體像打針一樣注入 DNA、新冠病毒透過受體融合、米米病毒則是打開星門），我們需要為每一種病毒量身打造專屬的 3D 動畫腳本。
> 
> 我建議我們先挑選 **1 到 2 款最具代表性、視覺效果最震撼的病毒** 來做為第一波的動畫實作，之後再陸續擴充。

## Open Questions

> [!WARNING]
> 請告訴我您希望先實作哪一款病毒的動畫？以下是幾個視覺效果極佳的推薦：
> 1. **T4 噬菌體 (Bacteriophage T4)**：降落於細菌表面 -> 尾部收縮 -> 像打針一樣將 DNA 注入。
> 2. **米米病毒 (Mimivirus)**：巨大的五角星門 (Stargate) 緩緩開啟 -> 釋放內部的膜質核心與遺傳物質。
> 3. **新冠病毒 (SARS-CoV-2)**：棘突蛋白 (Spike) 與底部的宿主細胞受體 (ACE2) 結合 -> 引發膜融合。

## Proposed Changes

### 1. UI 介面更新
#### [MODIFY] index.html
- 在右側資訊面板 (HUD) 或控制列中新增一個 **「▶ 播放感染動畫」** 按鈕。
- 新增一個半透明的字幕/提示框，用於在動畫播放時顯示當前的階段（例如：「正在與受體結合...」、「注入 DNA...」）。

### 2. 動畫引擎建置
#### [NEW] js/virusAnimations.js
- 建立一個專屬的動畫管理器 `AnimationController`。
- 支援使用 Three.js 的 `requestAnimationFrame` 與簡單的補間動畫 (Lerp/Tween) 來控制物件的位置、旋轉與形變。
- 實作特定病毒的動畫腳本（例如 `playT4Infection()`）。

#### [MODIFY] js/main.js
- 將按鈕事件綁定至 `AnimationController`。
- 在切換模式（全貌/剖面）或切換病毒時，自動重置/停止當前的動畫。

### 3. 場景擴充
#### [MODIFY] js/virusModels.js
- 為了配合動畫，部分模型在建立時需要將特定組件（如 T4 的尾鞘、米米病毒的星門）分離為獨立的 `THREE.Group`，以便在動畫中單獨控制它們的移動與縮放。
- 可能需要動態生成一個簡單的「宿主細胞膜 (Host Cell Membrane)」平面，做為病毒入侵的舞台。

## Verification Plan
1. 點擊「播放動畫」按鈕後，模型能流暢地執行指定動作，並且相機視角會自動微調以達到最佳觀賞角度。
2. 播放期間字幕能正確提示目前進度。
3. 切換病毒或重置視角時，動畫狀態能安全地中斷並恢復原始狀態，不會造成模型破圖或記憶體洩漏。
